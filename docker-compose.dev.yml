version: '3.8'

services:
  backend:
    container_name: backend
    build:
      context: ./backend
      dockerfile: dockerfile
    volumes:
      - ./backend:/backend
    ports:
      - "8000:8000"
    env_file:
      - .env_file
      